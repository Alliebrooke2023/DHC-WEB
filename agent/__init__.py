"""Agent — a Fable-5-style offline agent framework.

An agentic loop, an extensible tool registry, markdown skills, persistent
memory, and a terminal REPL — all runnable fully offline against local
models (Ollama), with an optional Anthropic API backend when online.
"""

__version__ = "0.1.0"
